import mongoose, {Schema} from 'mongoose';

const emailSchema = Schema({
    // write your shcema here
});

const Email = mongoose.model.Schemas || mongoose.model('email', emailSchema);

export default Email;